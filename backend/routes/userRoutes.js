import express from 'express'; import User from '../models/User.js'; import { authorize, protect } from '../middleware/auth.js'; const router=express.Router(); router.get('/',protect,authorize('admin'),async(req,res)=>res.json(await User.find().select('-password'))); router.patch('/:id/role',protect,authorize('admin'),async(req,res)=>res.json(await User.findByIdAndUpdate(req.params.id,{role:req.body.role},{new:true}).select('-password'))); router.delete('/:id',protect,authorize('admin'),async(req,res)=>{await User.findByIdAndDelete(req.params.id);res.json({message:'User deleted'})}); export default router
